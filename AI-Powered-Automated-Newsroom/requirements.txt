# AI Newsroom — Scraper + Embedding Pipeline dependencies
# Install: pip install -r requirements.txt

# ── HTTP & HTML parsing ────────────────────────────────────
requests==2.31.0
feedparser==6.0.11
beautifulsoup4==4.12.3
lxml>=5.2.2,<7               # fast BS4 parser (compatible with trafilatura 1.12)

# ── Article extraction (handles RTL, Arabic, multilingual) ─
# ENHANCEMENT: upgraded trafilatura from 1.6.4 → 1.12.0
# Significant Arabic/RTL improvements since 1.6.4 that directly
# benefit this project. Test on your sources before pinning further.
trafilatura==1.12.0
newspaper3k==0.2.8           # fallback
nltk==3.8.1                  # required by newspaper3k

# ── Database ───────────────────────────────────────────────
psycopg2-binary==2.9.9

# ── Date parsing ───────────────────────────────────────────
python-dateutil==2.9.0

# ── Environment variables ──────────────────────────────────
python-dotenv==1.0.1

# ── Embedding pipeline ─────────────────────────────────────
# Local sentence-transformer model (no external API calls)
sentence-transformers==3.0.1 # all-MiniLM-L6-v2 → 384-dim vectors
torch==2.3.1                 # CPU-only is fine; install CUDA variant for GPU
                             # GPU install: pip install torch --index-url https://download.pytorch.org/whl/cu121
transformers==4.41.2         # required by sentence-transformers
huggingface-hub==0.23.4      # model download/cache management

# ── Clustering ─────────────────────────────────────────────
scikit-learn==1.5.0          # DBSCAN + cosine metric
numpy==1.26.4                # array operations

# ── pgvector support ───────────────────────────────────────
pgvector==0.3.2              # psycopg2 adapter for the vector type

# ── Dev / testing ──────────────────────────────────────────
pytest==8.1.0
pytest-mock==3.12.0
